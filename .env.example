# Environment (copy to .env.local and fill in)
EXPO_PUBLIC_SENTRY_DSN=

# EAS Build only — upload source maps to Sentry (do not commit real token)
# eas secret:create --name SENTRY_AUTH_TOKEN --value "..."
SENTRY_AUTH_TOKEN=
