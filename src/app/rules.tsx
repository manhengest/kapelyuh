import { PlaceholderScreen } from '@ui/components/PlaceholderScreen';

export default function RulesScreen() {
  return (
    <PlaceholderScreen
      title="Правила"
      description="Карусель із 4 кроків з’явиться в Phase 2."
      links={[{ href: '/', label: 'На головну' }]}
    />
  );
}
